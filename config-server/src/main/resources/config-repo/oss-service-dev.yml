server:
  port: 9200

spring:
    servlet:
      multipart:
        max-file-size: 10MB
        max-request-size: 20MB

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
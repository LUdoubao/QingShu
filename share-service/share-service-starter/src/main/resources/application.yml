server:
  port: 9403

spring:
  application:
    name: share-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/share_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: Lg101799.
  redis:
    host: localhost
    port: 6379
    password:
    timeout: 5000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.share.model.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

share:
  aes:
    key: doubao
  base:
    url: https://www.example.com/share/

feign:
  client:
    config:
      default:
        connect-timeout: 5000
        read-timeout: 5000

logging:
  level:
    com.example.share: INFO
    org.springframework.cloud.openfeign: INFO
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.doubao.user.server.mapper.UserMapper">
    <!-- User findByEmail(@Param("email") String email); -->
    <select id="findByEmail" resultType="org.doubao.user.server.entity.User">
        SELECT * FROM user WHERE email = #{email}
    </select>

    <update id="updateUserAvatar">
        UPDATE user SET
            avatar_key = #{fileKey},
            storage_type = #{storageType}
        WHERE id = #{userId}
    </update>

    <!-- boolean findByUsername(@Param("username") String username); -->
    <select id="findByUsername" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM user WHERE username = #{username})
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.doubao.user.service.mapper.report.ReportCategoryMapper">

    <select id="selectAllEnabled" resultType="org.doubao.user.service.entity.report.ReportCategory">
        SELECT id, parent_id, name, level, sort, status
        FROM report_category
        WHERE status = 1
        ORDER BY level ASC, sort ASC
    </select>

    <select id="selectByParentId" parameterType="java.lang.Long" resultType="org.doubao.user.service.entity.report.ReportCategory">
        SELECT id, parent_id, name, level, sort, status
        FROM report_category
        WHERE parent_id = #{parentId}
        AND status = 1
        ORDER BY sort ASC
    </select>
</mapper>
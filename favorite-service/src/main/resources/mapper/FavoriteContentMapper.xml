<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.doubao.favorite.service.mapper.FavoriteContentMapper">
    <select id="selectByFolderId" resultType="org.doubao.favorite.service.entity.FavoriteContent">
        SELECT *
        FROM favorite_content
        WHERE folder_id = #{folderId}
        AND deleted = 0
    </select>

    <select id="selectByUserAndQuote" resultType="org.doubao.favorite.service.entity.FavoriteContent">
        SELECT *
        FROM favorite_content
        WHERE user_id = #{userId}
        AND quote_id = #{quoteId}
        AND deleted = 0
        LIMIT 1
    </select>

    <select id="countByFolderId" resultType="int">
        SELECT COUNT(*)
        FROM favorite_content
        WHERE folder_id = #{folderId}
        AND deleted = 0
    </select>

    <select id="countByUser" resultType="int">
        SELECT COUNT(*)
        FROM favorite_content
        WHERE user_id = #{userId}
        AND deleted = 0
    </select>

    <select id="selectFavoriteIdsByUserAndQuotes" resultType="java.lang.Long">
        SELECT quote_id
        FROM favorite_content
        WHERE user_id = #{userId}
        AND quote_id IN
        <foreach item="id" collection="quoteIds" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
    </select>

    <select id="selectByFolderIdPaged" resultType="org.doubao.favorite.service.entity.FavoriteContent">
        SELECT *
        FROM favorite_content
        WHERE folder_id = #{folderId}
        AND deleted = 0
        ORDER BY created_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 批量查询指定quote的收藏数量 -->
    <select id="countQuotes" resultType="org.doubao.favorite.service.dto.QuoteCountDTO">
        SELECT
        quote_id AS quoteId,
        COUNT(*) AS count
        FROM favorite_content
        WHERE quote_id IN
        <foreach item="id" collection="quoteIds" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
        GROUP BY quote_id
    </select>
</mapper>
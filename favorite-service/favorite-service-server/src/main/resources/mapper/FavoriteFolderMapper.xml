<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.doubao.favorite.service.mapper.FavoriteFolderMapper">

    <select id="selectUserFolders" resultType="org.doubao.favorite.service.entity.FavoriteFolder">
        SELECT *
        FROM favorite_folder
        WHERE user_id = #{userId}
        AND deleted = 0
        AND type = #{type}
        ORDER BY  updated_time DESC
    </select>
    <!-- Long selectFavoriteCount(Long folderId); -->
    <select id="selectFavoriteCount" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM favorite_content
        WHERE folder_id = #{folderId}
        AND deleted = 0
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.doubao.notification.service.mapper.NotificationMapper">

    <!-- 查询用户未读消息数量 -->
    <select id="selectUnreadCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM notification
        WHERE user_id = #{userId}
        AND status = 0
    </select>

    <!-- 批量标记消息为已读 -->
    <update id="batchMarkAsRead">
        UPDATE notification
        SET status = 1,
        updated_time = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- void allRead(@Param("userId") Long userId); -->
    <update id="allRead">
        UPDATE notification
        SET status = 1,
        updated_time = NOW()
        WHERE user_id = #{userId}
    </update>

</mapper>
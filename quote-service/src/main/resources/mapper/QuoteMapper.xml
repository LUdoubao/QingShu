<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.doubao.quote.service.mapper.QuoteMapper">
    <select id="countByTagIdsAndCategory" resultType="long">
        SELECT COUNT(DISTINCT q.id)
        FROM quote q
        WHERE q.deleted = 0
        AND q.status = 1
        <if test="categoryId != null">
            AND q.category_id = #{categoryId}
        </if>
        <if test="userId != null">
            AND q.created_id = #{userId}
        </if>
        <if test="original != null">
            AND q.original = #{original}
        </if>
        <if test="tagIds != null and !tagIds.isEmpty()">
            AND q.id IN (
            SELECT quote_id
            FROM quote_tag
            WHERE tag_id IN
            <foreach item="tagId" collection="tagIds" open="(" separator="," close=")">
                #{tagId}
            </foreach>
            GROUP BY quote_id
            HAVING COUNT(DISTINCT tag_id) = #{tagIdsSize}
            )
        </if>
    </select>
    <select id="selectByTagIdsAndCategory" resultType="org.doubao.quote.service.entity.Quote">
        SELECT q.*
        FROM quote q
        WHERE q.deleted = 0
        AND q.status = 1
        <!-- 分类条件（非空时生效） -->
        <if test="categoryId != null">
            AND q.category_id = #{categoryId}
        </if>
        <if test="userId != null">
            AND q.created_id = #{userId}
        </if>
        <if test="original != null">
            AND q.original = #{original}
        </if>
        <!-- 标签条件（非空且非空数组时生效） -->
        <if test="tagIds != null and tagIds.size > 0">
            AND q.id IN (
            SELECT quote_id
            FROM quote_tag
            WHERE tag_id IN
            <foreach item="tagId" collection="tagIds" open="(" separator="," close=")">
                #{tagId}
            </foreach>
            GROUP BY quote_id
            HAVING COUNT(DISTINCT tag_id) = #{tagIdsSize}
            )
        </if>
        ORDER BY q.created_time DESC
        LIMIT #{pageNum}, #{pageSize}
    </select>

    <select id="getQuoteType" resultType="Integer">
        SELECT q.original
        FROM quote q
        WHERE q.id = #{quoteId}
    </select>
    <!-- boolean checkQuoteExists(@Param("quoteId") String quoteId);-->
    <select id="checkQuoteExists" resultType="boolean">
        SELECT EXISTS(
        SELECT 1
        FROM quote q
        WHERE q.id = #{quoteId}
        AND q.deleted = 0
        AND q.status = 1
        )
    </select>

</mapper>
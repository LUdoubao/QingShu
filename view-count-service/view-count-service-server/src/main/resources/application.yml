server:
  port: 9402
spring:
  application:
    name: view-count-service
  datasource:
    url: jdbc:mysql://localhost:3306/view_count_db?useSSL=false&serverTimezone=UTC
    username: root
    password: Lg101799.
    driver-class-name: com.mysql.cj.jdbc.Driver
  profiles:
    active: dev
  config:
    import: "optional:configserver:"  # 关键修复！必须添加！
server:
  port: 9404
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: dialog_db
      username: root
      password: 123456
      authentication-database: admin
  application:
    name: dialog-service
  datasource:
    url: jdbc:mysql://localhost:3306/dialog_db?useSSL=false&serverTimezone=UTC
    username: root
    password: LG101799
    driver-class-name: com.mysql.cj.jdbc.Driver
  profiles:
    active: dev
  config:
    import: "optional:configserver:"  # 关键修复！必须添加！
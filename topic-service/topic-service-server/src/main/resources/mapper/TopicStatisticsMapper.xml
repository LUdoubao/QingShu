<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.doubao.topic.service.mapper.TopicStatisticsMapper">

    <update id="incrementQuoteCount">
        UPDATE topic_statistics
        SET quote_count = quote_count + 1,
        today_quote_count = today_quote_count + 1,
        updated_time = now()
        WHERE topic_id = #{topicId}
    </update>
    <update id="decrementQuoteCount">
        UPDATE topic_statistics
        SET quote_count = quote_count - 1,
        updated_time = now()
        WHERE topic_id = #{topicId}
    </update>
    <update id="decrementQuoteCountBatch">
        UPDATE topic_statistics
        SET quote_count = quote_count - 1,
        updated_time = now()
        WHERE topic_id IN
        <foreach item="item" index="index" collection="list"
                 open="(" separator="," close=")">
            #{item}
        </foreach>
    </update>


</mapper>
server:
  port: 9000

spring:
  config:
    import: "optional:configserver:"  # 关键修复！必须添加！
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/authdb?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: LG101799
    driver-class-name: com.mysql.cj.jdbc.Driver
  profiles:
    active: dev
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.doubao.search.service.mapper.SearchHistoryMapper">

    <select id="getUserSearchHistory" resultType="org.doubao.search.service.entity.SearchHistory">
        SELECT
            id,
            user_id,
            keyword,
            created_time
        FROM search_history
            WHERE id IN (
                SELECT MAX(id) AS id
                FROM search_history
                WHERE user_id = #{userId}
                AND deleted = 0
                GROUP BY keyword
            )
            ORDER BY
            (SELECT COUNT(*) FROM search_history s
            WHERE user_id = #{userId} AND keyword = s.keyword AND deleted = 0) DESC,
            created_time DESC
            LIMIT #{limit}
    </select>

</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.doubao.search.service.mapper.SearchIndexSyncTaskMapper">

    <select id="selectPendingTasks" resultType="org.doubao.search.service.entity.SearchIndexSyncTaskDO">
        SELECT *
        FROM search_index_sync_task
        WHERE task_status IN ('INIT', 'FAILED')
        ORDER BY id ASC
        LIMIT #{limit}
    </select>

    <update id="markSuccess">
        UPDATE search_index_sync_task
        SET task_status = 'SUCCESS',
            updated_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="markFailed">
        UPDATE search_index_sync_task
        SET task_status = 'FAILED',
            retry_count = retry_count + 1,
            fail_reason = #{failReason},
            updated_time = NOW()
        WHERE id = #{id}
    </update>
</mapper>
